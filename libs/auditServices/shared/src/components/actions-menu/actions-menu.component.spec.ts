import { ActionsMenuComponent } from './actions-menu.component';

describe('ActionsMenuComponent', () => {
  let component: ActionsMenuComponent;

  beforeEach(async () => {
    component = new ActionsMenuComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
