import { TextWithIconComponent } from './text-with-icon.component';

describe('TextWithIconComponent', () => {
  let component: TextWithIconComponent;

  beforeEach(async () => {
    component = new TextWithIconComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
