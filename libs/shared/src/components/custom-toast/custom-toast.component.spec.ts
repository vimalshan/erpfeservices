import { CustomToastComponent } from './custom-toast.component';

describe('CustomToastComponent', () => {
  let component: CustomToastComponent;

  beforeEach(async () => {
    component = new CustomToastComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
