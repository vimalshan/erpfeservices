import { EmptyChartComponent } from './empty-chart.component';

describe('EmptyChartComponent', () => {
  let component: EmptyChartComponent;

  beforeEach(() => {
    component = new EmptyChartComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
