import { FindingPreviousResponsesComponent } from './finding-previous-responses.component';

describe('FindingPreviousResponsesComponent', () => {
  let component: FindingPreviousResponsesComponent;

  beforeEach(async () => {
    component = new FindingPreviousResponsesComponent();
  });

  test('should create', () => {
    expect(component).toBeTruthy();
  });
});
