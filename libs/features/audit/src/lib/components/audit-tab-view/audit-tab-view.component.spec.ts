import { AuditTabViewComponent } from './audit-tab-view.component';

describe('AuditTabViewComponent', () => {
  let component: AuditTabViewComponent;

  beforeEach(async () => {
    component = new AuditTabViewComponent();
  });

  test('should create', () => {
    expect(component).toBeTruthy();
  });
});
